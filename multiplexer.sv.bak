module multiplexer(input logic cin, 
						input logic [3:0] S0, S1, 
						output logic Sout);

	if (cin == 0)
	begin
	Sout = S0;
	end
	
	else if (cin == 1)
	begin
	Sout = S1;
	end						

endmodule
						